# 锁文件有自己的规范格式, 不归 prettier 管
pnpm-lock.yaml
# worktree 工作区
.worktrees/
# superpowers 技能的工作区(plan brief / report / ledger 等运行期产物)。
# 它自己带一个 `*` 的 .gitignore,所以 git 已经不看它了;但 **prettier 只读顶层
# ignore 文件、不读嵌套 .gitignore**,不写在这里 `prettier --check .` 会因这些
# markdown 直接 exit 1,把门禁搞红。与上面 `.worktrees/` 同源处理。
.superpowers/
# justfile 用 just 语法, prettier 无对应 parser 会解析失败, 不归 prettier 管
justfile
# 挂载示例文档:markdown 内容, 并非 YAML 文件, prettier 会解析失败
plugins/obsolete/tool-vision/cordis.yml
# browser-operator 的 preset 模板: 不归 prettier 管。
# prettier 会重排 YAML(含 persona 的 `>-` 折叠块缩进, 那会改变语义); check-yaml 也因
# `!!js` 自定义标签把它排除了。同款口径见上面那条。
plugins/browser-operator/preset/
# 静态插件的 client bundle: 手写浏览器闭包, 不归 prettier 管
# (与 eslint.config.js 里 client bundle 整体豁免的约定一致)
plugins/ds-balance/lib/client.js
plugins/obsolete/paste-image/lib/client.js
plugins/obsolete/file-git-explorer/lib/client.js
plugins/db-console/lib/client.js
plugins/deepseek-harness/lib/client.js
plugins/batch-archive/lib/client.js
# deepseek-harness 引擎源码: 紧凑风格手写(shader/矩阵), 且经 build.mjs 拼入已忽略的
# lib/client.js —— 避免 prettier 重新排版导致 src 与产物不一致。不归 prettier 管。
plugins/deepseek-harness/src/
