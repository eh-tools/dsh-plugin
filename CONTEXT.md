# dsh-plugin(DeepSeek Harness 插件仓库)

本仓库承载 DSH 的静态双半插件(Host 半 `lib/index.js` + Client bundle `lib/client.js`),每个插件是 `plugins/` 下的独立 npm 包,经 `dsh plugin --profile web add link:<abs-path>` 挂载进 web profile。插件共享同一套挂载契约:`package.json` + `manifest.json` + `cordis.patch.yml`。

## file-git-explorer(左右树浏览插件)

**cwd**:
两个树面板共同的服务根目录。默认跟随**当前会话工作区**(会话切到哪个工作区, 树根就切到哪个工作区的目录, 经 `useSessions` 的 `byId[].cwd` 感知); 会话无 cwd(如 hero 空态)时回退 DSH 进程的当前工作目录。是文件树与 git 树的唯一事实来源, 随每个 API 请求以 `root` 参数下发。
_Avoid_: 工作区、项目根、目录树根

**可见组(visible group)**:
cwd 下既非点开头、也未被 .gitignore 忽略的条目,构成左侧上区的可显示树。
_Avoid_: 普通文件、正常文件

**隐藏组(hidden group)**:
cwd 下以 `.` 开头的条目(不含 `.git` 内部结构),构成左侧中区,与上区一样逐级懒加载展开。
_Avoid_: 点文件、dotfile

**忽略组(ignored group)**:
被 .gitignore 排除的条目(含被忽略的点文件),构成左侧最下方分区,逐级懒加载展开;另含**桥接目录**——自身未忽略但子树含忽略项的普通目录,否则深层忽略路径(如 `src/__pycache__`)在分区内不可达。
_Avoid_: 排除项、gitignore 列表

**悬浮面板(floating panel)**:
点击树节点后浮出的内容层,允许越过主对话区;左侧文件内容面板向右浮出,右侧 diff 面板与提交历史面板向左浮出(**后两者互斥**、共享同一锚位,打开其一即关闭另一;右树的分支下拉与这两者同样互斥——同占面板左侧留白带,开一关一防遮挡);左右两侧的浮层可同时存在、各自独立关闭;浮层与源侧栏联动(侧栏收起为细条时,对应悬浮栏一并关闭)。延迟收起的判定单位是「侧栏 + 其全部浮层」的**联合保持区**:鼠标在区域内任何成员上都不收起,同时离开所有成员才启动 360ms 宽限。
_Avoid_: 弹窗、tooltip、popover、预览卡片

**细条(strip)**:
树面板收起后的窄条形态,吸附在对应侧;悬停细条即展开该侧。与 DSH 自带侧边栏的收起态 rail 是两回事。
_Avoid_: rail

**图钉(pin)**:
树面板头部的按钮;**默认固定**;点任意一侧,两个面板都展开为卡片并锁定,再点解除。未钉住时两个面板以**悬停抽屉式**开合(悬停细条展开、鼠标移出延迟收起),固定时始终展开。固定态随 cwd 缓存(按仓库根)跨会话 / 刷新保留。**离场期间禁用**(见该词条):非对话视图一律按抽屉式处理,切回对话时还原进入前状态。
_Avoid_: 固定(易与"固定定位"混淆)

**离场(away)**:
当前会话激活视图 ≠ 对话的状态,经会话头部 tablist 的选中态观测。离场中两树强制收成细条(悬停细条仍可临时展开)、图钉禁用、全部悬浮栏关闭,进入前的固定/开合状态存入**内存快照**;切回对话即还原快照。轨迹与数据库页签一视同仁——fge 是这项联动的唯一事实来源,其他页签插件无需自行广播。快照只进内存不落盘。
_Avoid_: 页签监听(那是实现手段)、视图切换(那是用户动作)

**避让(strip clearance)**:
非对话视图下两侧细条对同页内容的让位约定——fge 把「该侧是否挂着可悬停细条」广播为根级 CSS 变量 `--dsh-fge-strip-clear-l/-r`(细条宽 26 + 吸边偏移 8 + 视觉间隙 6 = 40px,无细条时归零),数据库页据此内缩左右边界,使内容与可交互区不落入细条的悬停展开区;fge 卸载时变量随之移除。
_Avoid_: 边距(那是静态布局)、安全区(那是系统级概念)

**联动(linkage)**:
点击左侧文件时,若右侧 git 树存在该文件的 diff,滚动定位并高亮该节点;绝不自动打开 diff 面板。
_Avoid_: 跳转、聚焦、自动打开

**diff 范围(diff scope)**:
右侧变更列表的内容口径——工作区相对 HEAD 的全部变更(已暂存 + 未暂存 + 未跟踪),平铺展示、每项带状态徽标。
_Avoid_: 分支间差异(不是两分支之间的 diff)

**分支(branch)**:
右侧树顶部的只读展示;当前分支经自动/手动刷新实时读取,下拉列出本地 + 远程分组,不支持切换;点击非当前分支仅更新「查看分支」(见该词条),不改变仓库状态。提交历史面板头部的分支名按钮是同一「查看分支」的第二入口(同款分组菜单),两处任一点选均全局生效。
_Avoid_: 分支选择器、切换

**刷新(refresh)**:
git 状态刷新——在 **agent turn 结束**(会话 store 里 `running` 由 true→false)时自动重取 status(变更列表 + 当前分支)与已打开的历史面板(先比对 HEAD,变了才重拉),并更新 / 关闭已打开的 diff 悬浮栏(对应变更仍在则重拉,已不在变更集则关闭);有 1s 冷却,且**仅当右侧面板可见**时才真正执行(不可见则挂起、面板展开时补刷)。**手动 ⟳** 可无视冷却强制刷新并作废文件树缓存;**文件树本身仅靠手动 ⟳ 刷新**,不参与自动刷新。
_Avoid_: 手动刷新(那只是刷新的触发之一)、轮询、fs.watch(本插件未用)

**cwd 缓存(cwd cache)**:
按仓库根键控的 UI 状态记忆(仓库根 = cwd 向上找到的第一个 `.git` 所在目录;无仓库时退化为 cwd 本身),记「上次查看分支、面板宽度、展开状态」,切回同一仓库时恢复。分支事实(当前分支)始终实时读取,不进缓存。
_Avoid_: 分支缓存(缓存的是 UI 状态,不是分支事实)

**树面板(tree panel)**:
左右两个矩形面板本身;垂直方向在对话列内以对称边距居中,水平方向可拉伸但不覆盖主对话区。
_Avoid_: 侧栏、边栏(那是细条的称呼)

**文件搜索(name search)**:
左树的按名检索——对文件/目录的名称与相对路径做大小写不敏感的子串匹配,覆盖三区、带分区徽标地平铺命中项;不读取、不检索文件内容。
_Avoid_: 内容搜索、全文检索、grep

**提交历史(commit history)**:
右树头部入口浮出的悬浮面板:按时间倒序列出「查看分支」的提交(默认当前分支),点击单条可看完整提交说明与该次提交的变更内容;面板头部的分支名按钮可弹出分组菜单,直接改写「查看分支」并重拉列表。
_Avoid_: 日志、git log(那是实现命令)

**查看分支(viewed branch)**:
分支下拉里最后点击的分支,默认 = 当前分支;提交历史展示它的提交。只决定"看哪个分支的历史",不切换工作区的实际分支。
_Avoid_: 切换分支、上次查看(那只是该概念在 UI 上的标记)

**shell 行(shell bar)**:
左树底部常驻的命令执行行——单行输入框加「执行 ✓」「停止 ✕」两钮。Enter 与 ✓ 等价(含输入法守卫,并阻断按键向应用冒泡);执行后命令文本**保留不清空**;↑/↓ 在本仓库的历史命令间移动,历史随 cwd 缓存持久化。命令在 **cwd**(会话工作区)以**用户默认 shell** 执行,启动即挂入 DSH 后台任务列表(种类 `shell`,标签为命令原文);后台任务列表对人是**只读**的,故 ✕ 是人停止任务的唯一入口;完成**不惊动模型**。输出经**尾部输出窗**查看。
_Avoid_: 终端、terminal(不是交互式终端:无持续会话、无 cd 状态)、控制台

**用户默认 shell(default shell)**:
shell 行执行命令串的解释器——POSIX 取用户登录 shell,Windows 取 PowerShell;解析不到或不可用时按回退链降级到保底解释器。决定的是「用什么解释」,不改变任何其他语义。
_Avoid_: 系统 shell、环境 shell(都未指明是用户的默认这一含义)

**单槽(single slot)**:
shell 行的并发口径——同一时刻全局至多一条由它启动的任务在跑(按宿主侧记账,不按浏览器页面各自计);GUI 刷新/重开后仍在跑的任务照常显示并可被 ✕ 停止(整棵进程树 TERM → 宽限 → KILL)。运行中 ✓ 置灰;无运行时长上限。
_Avoid_: 任务队列、多开并发

**尾部输出窗(tail view)**:
shell 行上方可展开的输出查看区:滚动展示当前任务输出的**尾部**(更早内容不可见),运行中自动滚底,任务结束后保留终态输出与退出信息。
_Avoid_: 终端模拟器、完整日志

## db-console(数据库控制台插件)

**数据库页签(database view)**:
注册进会话头部的第三个视图(id `database`),与对话、轨迹平级、排在轨迹之后。进入时左右两树收成细条,切回对话时按快照还原树面板状态。
_Avoid_: 数据库标签页、DB 标签

**数据库连接(database connection)**:
项目级**单例**——隔离键与「cwd 缓存」同口径(仓库根 = cwd 向上找到的第一个 `.git` 所在目录,无仓库退化为 cwd 本身),一个项目至多一条,以完整 PostgreSQL 链接串描述,**明文**持久化在 Host 侧(UI 打码展示、配置文件权限收紧);切换项目即面对另一条连接。没有连接列表、没有命名连接,也不做加密。
_Avoid_: 多连接、连接管理器、工作区级(隔离的是项目,不是 DSH 会话工作区)、加密(明确放弃)、主口令(随加密一并放弃)
