# dsh-plugin(DeepSeek Harness 插件仓库)

本仓库承载 DSH 的插件:需要浏览器 UI 的是**静态双半插件**(Host 半 `lib/index.js` + Client bundle `lib/client.js`),纯功能性的可以是**纯 host 插件**(`package.json` + `lib/index.js` + `tests/`,无 client 半)。静态双半插件经 `dsh plugin --profile web add link:<abs-path>` 挂进 web profile,共享同一套挂载契约(`package.json` + `manifest.json` + `cordis.patch.yml`);纯 host 插件挂进 **agent preset**(见下方 `browser-operator`)。

## file-git-explorer(右侧栏 git 页签 + 悬浮面板 + 终端抽屉)

> **v0.7 重构**:文件树**回归官方** —— 本插件既不接管、也不自绘文件树。详情不再开成右栏页签,而是浮到右栏**左侧**。
> 随之移除的概念:自带文件树(可见 / 隐藏 / 忽略三区)、文件搜索、编辑保存、`.http` 运行、shell 行 / 单槽 / 尾部输出窗、细条、图钉、终端页签,以及 `files-lite`、`doc-copy` 两个插件。
> 下表只列仍然成立的词条;旧词条不再有效,出现即属误用。

**右侧栏页签(right sidebar tab)**:
官方右侧栏里的一格视图,与官方文件树、官方文档预览平级并列。本插件自己注册的只有「git 页签」与「diff 页签」两种。
新会话的右栏**默认铺两格**: 官方「工作区文件」+ 本插件的「git 页签」,且**后者是活动的那一格**(打开右栏直接看变更列表)。`Alt+J` / `Alt+L` 选左 / 右相邻的一格,**到边不环绕**。
_Avoid_: tag 侧栏、标签页、栏、面板

**git 页签(git tab)**:
本插件的常驻页签,正文是**上下两栏**(见「git 页签分栏」): 上栏**变更列表**,下栏在**提交历史列表**与**聚焦提交**之间二选一。只读 —— 不改仓库状态、也不切换分支。
_Avoid_: 右树、git 面板

**变更列表(change list)**:
工作区相对 `HEAD` 的全部变更(已暂存 + 未暂存 + 未跟踪),每项带一个单字母徽标,按路径排序、未跟踪沉底;
上屏时**按目录归类**(见「目录归类」)。
_Avoid_: diff 列表、文件树、改动列表

**diff 范围(diff scope)**:
**工作区相对 HEAD** 的变更 —— 不是两分支之间、也不是两次提交之间的差异;从提交展开出来的 diff 则指「该提交相对其父提交」的那一次改动。未跟踪文件没有 diff 可比。
_Avoid_: 分支间差异、提交间差异

**提交历史(commit history)**:
git 页签**下栏**里按时间倒序列出「查看分支」提交的列表(即**历史列表态**)。点一条提交**聚焦**它 ——
下栏整栏换成那一条,顶上带**返回**;列表**不再就地展开**,聚焦里也不再有第二条提交。
_Avoid_: 日志、git log(那是实现命令)

**聚焦提交(focused commit)**:
git 页签下栏的另一种形态(即**聚焦态**): 整栏只有**一条**提交 —— 顶上一条**标题栏**(`← 返回` + `作者 · 时间` + hash 胶囊),
下面依次是**提交说明**与**按目录归类**的文件清单(带 ±行数),点文件照样进**悬浮面板**。
进聚焦时下栏自动放到最大(60%),返回时还回进聚焦前的比例 —— 但**聚焦期间自己拖过拖柄**的话,以那个新值为准。
返回**不重新拉取、也不补刷**,并把列表滚回你离开时那一处。它与列表态、变更列表一样**按会话缓存**。
_Avoid_: 提交详情(「详情」已被悬浮面板占用)、预览、单提交视图、页

**历史列表态 / 聚焦态(list state / focused state)**:
git 页签**下栏**的两种形态, 二选一:「历史列表态」= **提交历史**列表(默认),「聚焦态」= **聚焦提交**。
两个形态**共用同一个滚动容器**, 进出时滚动位置与分栏比例都会被摆正。
_Avoid_: 视图栈、页面、子页

**提交说明(commit message)**:
**聚焦提交**里最上面那块说明。默认**折成两行**(放不下才给「展开 / 收起」,短说明没有这枚开关),
是否放得下按实际高度量、不按行数猜。开关**独占一行、左对齐、在说明文字上方** —— 展开后它原地不动,
不用再拉滚动条去找「收起」。展开态**自己滚**(最多约 14 行),不把下栏撑长、也不让整栏跟着重排。
展开态与「聚焦的是哪一条」一起按会话缓存。
_Avoid_: 描述、说明文字、log 正文

**hash 胶囊(hash chip)**:
**提交历史**每行第二行末尾那枚短 hash。**点一下复制完整 hash**(不是显示的那个短的, 粘进终端就能 `git show`),
复制后它自己变成「已复制 / 复制失败」, 1.2s 后变回短 hash; 这一下**不会**连带**聚焦**这条提交。
**聚焦提交**的标题栏里也有同一枚。
_Avoid_: hash 标签、hash 徽标、badge(那是变更行首的 M/A/D)

**git 页签分栏(git tab split)**:
git 页签正文的**上下两栏** —— 上栏是**变更列表**(当前 diff),默认占正文 **3/4** 高;下栏是**提交历史列表**或**聚焦提交**。两栏**各自滚动**;
中间那条拖柄可调占比(双击回到 3/4),拖过的值记在 `localStorage`。可拖区间 `[40, 90]`:上限是"下栏至少留一条",
下限是**下栏最多 60%**(正文 = 整页减 38px 页签头,所以对整页只会更严)。比例是**整个页签**的偏好,不按会话分。
_Avoid_: 分栏(那是官方右栏的 split 模式)、折叠区、面板

**标题栏(title bar)**:
git 页签**上下两栏**各自顶上那条(「变更列表 N」「提交历史 · 查看 X」,以及**聚焦提交**的返回条):
**不透明的实色横条** + 下边一条细线 —— 滚动的内容被它挡在下面, 不会从字缝里透出来; 吸顶(sticky)。
它的"不透明"来自**底座那层实色**(面板自己的面), 主题强调色只是叠上去的一层(强调色可以半透明 —— 那是主题的自由)。
⚠ 「终端抽屉」顶上那条叫**终端标题条**, 是另一个东西, 别用同一个名字。
_Avoid_: 头部、节头、分区头、header

**目录归类(directory grouping)**:
两份列表共用的排布:把路径折成一棵**目录树** —— 同一个目录只出现一次目录行(带文件夹图标),文件名以 **basename** 缩进列在它下面
(`a/a1`、`a/a2` → 目录行 `a`,底下 `a1`、`a2`);**一层只套一个目录、自己又没有文件的链并成一行**(`plugins/file-git-explorer/lib`)。
目录在前、文件在后;完整路径仍在 `title` 里,diff 请求用的也是完整路径。层级由**层级线**画出来。
_Avoid_: 分组、平铺、缩进列表

**层级线(indent guide)**:
目录归类里那条**纵向虚线**: 每一层一条,画在那个目录的文件夹图标下方,从目录行一直连到它**最后一个子项的行中**;
子项不再是最后一个、或祖先已经收尾时,线就在该收的地方断开。
_Avoid_: 缩进线、辅助线、参考线

**查看分支(viewed branch)**:
提交历史所跟随的分支,默认 = 当前分支。它只决定「看哪个分支的历史」,**不切换工作区的实际分支**。
_Avoid_: 切换分支、上次查看

**刷新(refresh)**:
**自动刷新**在 agent turn 结束时触发,只重取变更列表与已打开的提交历史;git 页签不可见时挂起、可见时补刷;
**聚焦提交期间挂起且不补刷**(返回列表时不会把背后的列表换掉),下一次 turn 结束或手动 ⟳ 才刷。自动刷新**不做网络请求**。
**手动 ⟳** 先 `git fetch --all --prune`,再重取状态。
_Avoid_: 轮询、fs.watch(本插件未用)、手动刷新(那只是刷新的触发之一)

**悬浮面板(floating panel)**:
从右侧栏**左缘向左**浮出的详情面板:视口 1/2 宽、满高、与右栏正对齐。**同一时间只有一个** —— 打开第二个会顶掉第一个,看起来就是内容就地更换。切换会话、切换工作区、右栏折叠都会关掉它。**详情只以浮层出现**:页签条上不会先冒出一枚详情页签再消失;浮起之后右栏**停在用户原本选中的那一格**(从「文件」点文件仍是「文件」,不会跳到 git 树)。
_Avoid_: 悬浮栏、浮层、弹窗、抽屉、侧栏

**详情(detail)**:
悬浮面板里承载的东西本身 —— 一份**文件内容**,或一份 **diff**。
⚠ 与「聚焦提交」是两回事:详情住在**悬浮面板**里、由点文件或聊天里的文件链接触发;聚焦提交住在 **git 页签下栏**里、由点提交历史的一条触发。
_Avoid_: 预览(那是官方文档页签的说法)、视图、提交详情

**右栏宽度(rightbar width)**:
本插件对**官方右栏宽度的覆盖**,一处:左缘那根拖柄**可拖**,范围 **200px ~ 15% 视口**,拖过的值记在 `localStorage`(刷新 / 切会话保持),没拖过就是上限 15vw。宽度是**真实的一格轨道** —— 展开右栏是**把中栏挤窄**,折叠时整格还给中栏。
_Avoid_: 面板宽度(那是悬浮面板的)、分栏比例、缩放

**composer 座(composer dock)**:
composer 卡片**下方**的环境条座位。它属于对话区,不属于官方右侧栏体系。终端抽屉的收起态落户在这里。
_Avoid_: 插件区域、底部栏、dock、面板位

**抽屉舌(drawer tongue)**:
终端抽屉收起态在 composer 座里留下的那枚小三角。点击**向上**展开抽屉。
_Avoid_: 小箭头、把手、拉手、标签

**终端抽屉(terminal drawer)**:
从 composer 座**向上**展开的终端。**宽度与 composer 卡片完全一致**(照官方那两个变量算:`--dsh-composer-side-clearance` 与 `--dsh-composer-card-max-width`;不是正文列宽 `--dsh-chat-content-width`)。展开高度由**上缘拖柄**调整(20%~70%,按工作区记忆)。顶上是**终端标题条**:页签上的 `×` 只是收起抽屉、**不杀进程**;`■` 才终止整棵终端进程树。终端里**选中即复制**(拖选 / 双击选词 / 三击选行, 一松手就进剪贴板), **Alt+C** 是兜底(见「终端选中即复制」);Ctrl+C 仍原样送给 PTY 当 SIGINT)。
_Avoid_: 底部面板、终端页签、fixed 悬浮

**终端选中即复制(copy-on-select)**:
终端抽屉里的**默认复制路径** —— 拖选 / 双击选词 / 三击选行一松手(`mouseup`)就把选区写进剪贴板, 不用按键。
**Alt+C 是兜底**: 内容与自动那次相同时也**强制重写一遍**(自动那次可能被浏览器或权限挡了, 这时内容当然一模一样, 不能跳过)。
两条路共用同一条 `copySelection(force, expandTail)`, 都走官方 `primitives.writeClipboard`。
**终端标题条右侧那枚开关**(`role="switch"`)关掉的是**自动**这条: Alt+C 照旧、选区收敛照旧, 状态记在 `fge-term-copy-v1`(默认开)。
⚠ 读选区与发起写入在 `mouseup` 里**同步**做完(剪贴板 API 要"用户手势"); 监听挂**终端体**而非 `document`;
⚠ 它长在"整条可点即收起"的标题条里, 所以点击必须 `stopPropagation`。
_Avoid_: 自动复制、Alt+C 复制(那只指兜底那一条)、Ctrl+C 复制(那是 SIGINT)、开关=关掉复制(它只关自动那条)

**选区尾巴收敛(selection tail clamp)**:
指"选区不许漫到**最后一行有内容**之下"这条口径 —— 终端格子是按 `rows × cols` 画的, **光标下面本来就还有一整片空格**,
放任的话一拖到底就选中一大堆空行、复制出一串换行(用户报的 bug)。三件事:
**拖拽途中**在 `document` **捕获阶段**吃掉落在"内容下方"的 `mousemove`(xterm 的拖拽监听在冒泡阶段),
**松手时**把尾巴收到那一行(`clampSelectionTail`), 而**中间的空行照留**(`findLastContentRow` 只找最深那行有内容的)。
⚠ 只在 `mouseup` / Alt+C 落选区: xterm 的 `setSelection` 会 `_removeMouseDownListeners()`, 拖拽途中调用会把拖拽弄断。
_Avoid_: 清空尾部、trim(那说的是复制出来的字符串; 这里是**选区本身**)、自动裁剪空格(xterm 自己每行都会右裁)

**终端标题条(terminal title bar)**:
终端抽屉顶上的那条标题区,外观照 Windows Terminal 的页签栏做 —— 一枚页签(`>_` 字形 + **尾部省略号**截断的工作区路径 + 悬停出现的 `×`),右端一枚 `■`。条里**恒定只有当前工作区这一枚页签**:它长得像页签,但**不是多页签容器**,每工作区仍是一个终端(见「工作区终端」)。点条空白处与点页签上的 `×` 等价(收起抽屉,不杀进程)。页签与 `■` 在**拖柄 + 标题条**这条带子里**垂直居中**(不贴底边那条线)。
_Avoid_: 终端页签、多标签栏、标签页(都指一个容器装多个终端,本插件没有)

**工作区终端(workspace terminal)**:
按归一化 + 大小写折叠的 cwd 键控的常驻 PTY,**每工作区一个**;同工作区的多个客户端**共享同一终端**(输出广播,任意一端皆可输入)。跨抽屉关闭 / 切会话 / 页面刷新存活。
_Avoid_: 会话终端、shell 行、后台任务(那是 `ctx.jobs` 的概念)

**回放缓冲(replay buffer)**:
每个工作区终端保留的**尾部输出**,供重开抽屉 / 刷新页面重连时补发。用**绝对字节位**寻址,因此不因缓冲修剪而错位。
_Avoid_: 日志、scrollback(xterm 自身的滚动区是另一回事)

**工作区(workspace)**:
本插件的 cwd —— 当前会话的工作区目录,会话没有 cwd 时回退 DSH 进程的当前工作目录。它是 git 状态与工作区终端的**唯一**根。
_Avoid_: 项目根、仓库根(仓库根是它向上找到的第一个 `.git`,是另一个概念)

## db-console(数据库控制台插件)

**数据库页签(database view)**:
注册进会话头部的第三个视图(id `database`),与对话、轨迹平级、排在轨迹之后。它曾与 file-git-explorer 的树面板联动(进入收细条、切回按快照还原),fge 移除面板后该联动已不存在,本视图不再需要协调。
_Avoid_: 数据库标签页、DB 标签

**数据库连接(database connection)**:
项目级**单例**——隔离键取「仓库根」(cwd 向上找到的第一个 `.git` 所在目录,无仓库退化为 cwd 本身),一个项目至多一条,以完整 PostgreSQL 链接串描述,**明文**持久化在 Host 侧(UI 打码展示、配置文件权限收紧);切换项目即面对另一条连接。没有连接列表、没有命名连接,也不做加密。
_Avoid_: 多连接、连接管理器、工作区级(隔离的是项目,不是 DSH 会话工作区)、加密(明确放弃)、主口令(随加密一并放弃)

## browser-operator(浏览器操作插件 + 预设)

**浏览器会话(browser session)**:
本插件在宿主进程里持有的那**一个** Playwright `BrowserContext`——有头可见、跨对话轮次存活、dsh 重启后重建。预设是 standing mount(每进程一份),所以同时刻只有这一个会话;它是惰性创建的(首次 `browser_*` 调用才拉起),`dispose` 时关掉。
_Avoid_: 浏览器实例、浏览器进程、tab、窗口(窗口是它的外观,不是它的身份)

**独立浏览器 profile(dedicated browser profile)**:
浏览器会话专用的 user-data-dir(`$DSH_HOME/browser-operator/profile`),与用户日常浏览器的 profile **完全无关**。登录态就存在这里,所以「登录一次长期复用」;也正因为它独立,Playwright 能带自己的 `--user-data-dir` 起新进程,不必碰调试端口。
_Avoid_: 用户 profile、默认 profile、Chrome 用户数据(那说的是日常浏览器那一份)

**浏览器操作预设(browser-operator preset)**:
`~/.dsh/.agent-presets/browser-operator/`,从 shipped `standard` 复制而来、只改两处的 agent preset:人设换成浏览器操作员(并在人设里申明产物目录规则),末尾追加一行 `browser-operator` 插件。它的存在理由是给一个会话装上那 9 个工具;插件本身是**纯 host 插件**,不做成 profile bundle。
_Avoid_: 浏览器插件(那是 profile 层的说法)、浏览器模式、browser profile

**浏览器产物目录(browser artifact directory)**:
截图等产物落盘的那个目录,**按会话工作区**解析、**优先选项目已经 ignore 的目录**(`logs/`、`output/`、`scripts/` 等)下的 `browser-operator/` 子目录;仓库里找不到就退到 `$DSH_HOME/browser-operator/<session-id>`,**绝不**往仓库写没被 ignore 的东西。判定以 `git check-ignore` 为准,git 不可用时退化到 `.gitignore` 解析。
_Avoid_: 截图目录、输出目录、workdir、临时目录(它是有规则的,不是随便一个 tmp)
